export const Route = createFileRoute({
  component: TaskPage,
});

function TaskPage() {
  return (
    <div className="flex h-full items-center justify-center">
      TODO: Task Page
    </div>
  );
}
