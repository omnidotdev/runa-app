fragment ProjectFragment on Project {
  id
  rowId
  name
  description
  prefix
  color
  labels
  workspaceId
  columns {
    nodes {
      rowId
    }
  }
  viewMode
  createdAt
  updatedAt
  workspace {
    ...WorkspaceFragment
  }
}
