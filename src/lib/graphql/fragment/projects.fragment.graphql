fragment Project on Project {
  rowId
  name
  description
  color
  prefix
  projectColumnId
  columns {
    nodes {
      allTasks: tasks {
        totalCount
      }

      completedTasks: tasks(
        filter: { column: { title: { equalTo: "Done" } } }
      ) {
        totalCount
      }
    }
  }
}
