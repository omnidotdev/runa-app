mutation CreateColumn($input: CreateColumnInput!) {
  createColumn(input: $input) {
    clientMutationId
    column {
      ...ColumnFragment
    }
    query {
      columns(first: 10) {
        nodes {
          ...ColumnFragment
        }
      }
    }
  }
}
