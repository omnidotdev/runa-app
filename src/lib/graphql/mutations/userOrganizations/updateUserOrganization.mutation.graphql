mutation UpdateUserOrganization($input: UpdateUserOrganizationInput!) {
  updateUserOrganization(input: $input) {
    userOrganization {
      rowId
      organizationId
      slug
      name
      type
      role
    }
  }
}
