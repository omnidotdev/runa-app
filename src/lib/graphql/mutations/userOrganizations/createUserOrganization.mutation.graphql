mutation CreateUserOrganization($input: CreateUserOrganizationInput!) {
  createUserOrganization(input: $input) {
    userOrganization {
      rowId
      organizationId
      slug
      name
      type
      role
    }
  }
}
