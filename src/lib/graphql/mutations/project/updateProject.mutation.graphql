mutation UpdateProject($input: UpdateProjectInput!) {
  updateProject(input: $input) {
    clientMutationId
    project {
      ...ProjectFragment
    }
    query {
      projects(first: 10) {
        nodes {
          ...ProjectFragment
        }
      }
    }
  }
}