mutation CreateProject($input: CreateProjectInput!) {
  createProject(input: $input) {
    clientMutationId
    project {
      ...ProjectFragment
    }
    query {
      projects(first: 10) {
        nodes {
          ...ProjectFragment
        }
      }
    }
  }
}