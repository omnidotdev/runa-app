mutation DeleteProject($input: DeleteProjectInput!) {
  deleteProject(input: $input) {
    clientMutationId
    deletedProjectId
    query {
      projects(first: 10) {
        nodes {
          ...ProjectFragment
        }
      }
    }
  }
}