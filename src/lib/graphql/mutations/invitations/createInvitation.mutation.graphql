mutation CreateInvitation($input: CreateInvitationInput!) {
  createInvitation(input: $input) {
    invitation {
      rowId
      organizationId
      email
      role
      token
      status
      expiresAt
    }
  }
}
