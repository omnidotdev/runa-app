mutation UpdateUser($input: UpdateUserInput!) {
  updateUser(input: $input) {
    clientMutationId
    user {
      ...UserFragment
    }
    query {
      users(first: 10) {
        nodes {
          ...UserFragment
        }
      }
    }
  }
}
