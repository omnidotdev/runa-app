query Posts($first: Int, $filter: PostFilter, $orderBy: [PostOrderBy!]) {
  posts(first: $first, filter: $filter, orderBy: $orderBy) {
    nodes {
      ...PostFragment
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
