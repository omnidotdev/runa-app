query Projects(
  $first: Int
  $filter: ProjectFilter
  $orderBy: [ProjectOrderBy!]
) {
  projects(first: $first, filter: $filter, orderBy: $orderBy) {
    nodes {
      ...ProjectFragment
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
