query Tasks($columnId: UUID!) {
  tasks(condition: { columnId: $columnId }) {
    nodes {
      rowId
      content
      priority
      dueDate
      labels
      assignees {
        nodes {
          rowId
          user {
            name
          }
        }
      }
    }
  }
}
