query Tasks(
  $projectId: UUID!
  $search: String = ""
  $assignees: TaskToManyAssigneeFilter
) {
  tasks(
    filter: {
      projectId: { equalTo: $projectId }
      content: { includesInsensitive: $search }
      assignees: $assignees
    }
  ) {
    nodes {
      rowId
      projectId
      columnId
      columnIndex
      content
      priority
      dueDate
      labels
      assignees {
        nodes {
          rowId
          user {
            rowId
            name
            avatarUrl
          }
        }
      }
    }
  }
}
