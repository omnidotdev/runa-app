query Task($rowId: UUID!) {
  task(rowId: $rowId) {
    rowId
    projectId
    columnId
    columnIndex
    content
    description
    priority
    createdAt
    updatedAt
    dueDate
    taskLabels {
      nodes {
        rowId
        label {
          ...Label
        }
      }
    }
    posts(orderBy: CREATED_AT_ASC) {
      totalCount
      nodes {
        rowId
        title
        description
        createdAt
        authorId
        author {
          name
          avatarUrl
          rowId
          id
        }
      }
    }
    column {
      title
      emoji
    }
    author {
      name
      avatarUrl
      rowId
    }
    assignees {
      nodes {
        rowId
        user {
          rowId
          name
          avatarUrl
        }
      }
    }
  }
}
