query Invitations($email: String) {
  invitations(condition: { email: $email }) {
    nodes {
      rowId
      workspace {
        rowId
        name
        projects {
          nodes {
            rowId
          }
        }
        workspaceUsers {
          totalCount
        }
      }
    }
  }
}
