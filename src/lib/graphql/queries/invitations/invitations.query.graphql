query Invitations($email: String) {
  invitations(condition: { email: $email }) {
    nodes {
      rowId
      workspace {
        rowId
        organizationId
        projects {
          nodes {
            rowId
          }
        }
        members {
          totalCount
        }
      }
    }
  }
}
