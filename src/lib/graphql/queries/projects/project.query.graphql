query Project($rowId: UUID!) {
  project(rowId: $rowId) {
    rowId
    name
    slug
    description
    prefix
    isPublic
    projectColumnId
    nextTaskNumber
    color
    tasks {
      totalCount
    }
    columns(orderBy: INDEX_ASC) {
      nodes {
        rowId
        index
        title
        icon
        tasks {
          totalCount
        }
      }
    }
  }
}
