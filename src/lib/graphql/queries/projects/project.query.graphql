query Project($rowId: UUID!, $columns: [UUID!]) {
  project(rowId: $rowId) {
    rowId
    name
    description
    prefix
    color
    projectColumnId
    labels {
      nodes {
        name
        color
        rowId
      }
    }
    tasks {
      totalCount
    }
    columns(filter: { rowId: { notIn: $columns } }, orderBy: INDEX_ASC) {
      nodes {
        rowId
        index
        title
        emoji
        tasks {
          totalCount
          nodes {
            rowId
            createdAt
          }
        }
      }
    }
  }
}
