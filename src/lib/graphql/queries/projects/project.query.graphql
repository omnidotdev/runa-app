query Project($rowId: UUID!) {
  project(rowId: $rowId) {
    rowId
    name
    description
    prefix
    color
    columns {
      nodes {
        rowId
        title
        tasks {
          totalCount
          nodes {
            rowId
          }
        }
      }
    }
  }
}
