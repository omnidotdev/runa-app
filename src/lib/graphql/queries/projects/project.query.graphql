query Project($rowId: UUID!) {
  project(rowId: $rowId) {
    rowId
    name
    description
    prefix
    tasks {
      nodes {
        rowId
      }
    }
    columns {
      nodes {
        rowId
        title
        tasks {
          totalCount
        }
      }
    }
  }
}
