query Project($rowId: UUID!) {
  project(rowId: $rowId) {
    rowId
    name
    description
    prefix
    columns {
      nodes {
        rowId
        title
        tasks {
          totalCount
        }
      }
    }
  }
}
