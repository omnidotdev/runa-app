query Project($rowId: UUID!) {
  project(rowId: $rowId) {
    rowId
    name
    slug
    description
    prefix
    color
    viewMode
    labels {
      nodes {
        name
        color
        rowId
      }
    }
    columns(orderBy: INDEX_ASC) {
      nodes {
        rowId
        index
        title
        tasks {
          totalCount
          nodes {
            rowId
            createdAt
          }
        }
      }
    }
  }
}
