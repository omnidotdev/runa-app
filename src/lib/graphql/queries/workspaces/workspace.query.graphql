query Workspace($rowId: UUID!) {
  workspace(rowId: $rowId) {
    rowId
    name
    viewMode
    projectColumns {
      nodes {
        emoji
        rowId
        title
      }
    }
    projects {
      nodes {
        rowId
        name
        color
        viewMode
        prefix
        projectColumn {
          title
          emoji
        }
        tasks {
          totalCount
        }
        columns {
          nodes {
            allTasks: tasks {
              totalCount
            }

            completedTasks: tasks(
              filter: { column: { title: { equalTo: "Done" } } }
            ) {
              totalCount
            }
          }
        }
      }
    }
    workspaceUsers {
      nodes {
        userId
      }
    }
  }
}
