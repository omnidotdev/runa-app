import { code } from "@streamdown/code";

import type { PluginConfig } from "streamdown";

/** Streamdown plugin configuration for rendering agent markdown. */
export const streamdownPlugins: PluginConfig = { code };
