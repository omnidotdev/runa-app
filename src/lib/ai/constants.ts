/** Tool names that perform write operations on the board. */
export const WRITE_TOOL_NAMES = new Set([
  "createTask",
  "updateTask",
  "moveTask",
  "assignTask",
  "addLabel",
  "removeLabel",
  "addComment",
]);
